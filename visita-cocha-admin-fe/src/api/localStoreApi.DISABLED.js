/**
 * 🔥 ARCHIVO LOCAL STORAGE DESHABILITADO
 * 
 * Este proyecto ahora usa SOLO datos reales del backend.
 * Este archivo ya no se usa.
 * 
 * Si ves errores relacionados con localStoreApi, actualiza el import a:
 * import { getContentList, createContent, updateContent, deleteContent } from '../../api';
 */

throw new Error('🔥 localStoreApi está deshabilitado. Usa el backend real configurando VITE_USE_BACKEND=true');
